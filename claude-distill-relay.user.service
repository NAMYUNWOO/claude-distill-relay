[Unit]
Description=Claude Distill Relay Server (user)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=%h/.openclaw/workspace/claude-distill-relay
Environment=PYTHONUNBUFFERED=1
EnvironmentFile=-%h/.openclaw/workspace/claude-distill-relay/.env.relay
ExecStart=%h/.openclaw/workspace/claude-distill-relay/.venv/bin/python %h/.openclaw/workspace/claude-distill-relay/relay.py --host ${RELAY_HOST} --port ${RELAY_PORT} --max-rooms ${RELAY_MAX_ROOMS} --room-ttl ${RELAY_ROOM_TTL} --max-msg-size ${RELAY_MAX_MSG_SIZE} --rate-limit-max ${RELAY_RATE_LIMIT_MAX} --rate-limit-window ${RELAY_RATE_LIMIT_WINDOW} --log-dir ${RELAY_LOG_DIR}
Restart=always
RestartSec=3
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ProtectHome=false
ReadWritePaths=%h/.openclaw/workspace/claude-distill-relay

[Install]
WantedBy=default.target
